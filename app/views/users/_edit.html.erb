<% @user.password = @user.password_confirmation = nil %>
<% @user.password = @user.password_confirmation = nil %>
<%= form_for @user, :url => {:controller => "users", :action => @user.new_record? ? "create" : "update" } , :html => { :method => @user.new_record? ? "post" : "put", :class=>"content-box account-tabs" } do |f|%>
  <%= render :partial => "form", :locals =>{:f => f} %>
<% end %>
